<nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
    <a class="navbar-brand" href="@Url.Action("Index","Home")">Navette Express</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="@Url.Action("Index","Home")"><i class="fas fa-home"></i> Acceuil</a>
            </li>
            @if (ViewBag.UsrSession != null && ViewBag.UsrSession.role_ == "client")
            {
<li class="nav-item">
    <a class="nav-link" href="@Url.Action("About", "Home")">About</a>
</li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Contact", "Home")">Contact</a>
                </li>            }
            @if (ViewBag.UsrSession != null && ViewBag.UsrSession.role_ == "societe")
            {
<li class="nav-item">
    <a class="nav-link" href="@Url.Action("dashboard", "Societes")">Dashboard</a>
</li>            }
            @if (ViewBag.UsrSession != null && ViewBag.UsrSession.role_ == "admin")
            {
<li class="nav-item">
    <a class="nav-link" href="@Url.Action("Index", "Utilisateurs")">Dashboard</a>
</li>            }
        </ul>
        @if (ViewBag.UsrSession != null)
        {
<ul class="navbar-nav">
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown"><i class="far fa-user-circle"></i> Mon Compte</a>
        <div class="dropdown-menu">
            <h4 class="dropdown-item" style="background-color:#296d98;color:floralwhite"><i class="far fa-user"></i> @ViewBag.UsrSession.nom</h4>
            <div class="dropdown-divider"></div>
            <a href="@Url.Action("Edit", "Utilisateurs", new { id = ViewBag.UsrSession.id})" class="dropdown-item"><i class="far fa-user-circle"></i> Mon Compte</a>
            <div class="dropdown-divider"></div>
            <a href="@Url.Action("sedeconncter","Utilisateurs")" class="dropdown-item"><i class="fas fa-sign-out-alt"></i> Se deconnecter</a>
        </div>
    </li>
</ul>}
        @if (ViewBag.UsrSession == null)
        {
<ul class="navbar-nav">
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("Login","Utilisateurs")"><i class="fas fa-user" data-toggle="tooltip" data-placement="bottom" title="Mon compte"></i> Se connecter</a>
    </li>
</ul>}
    </div>
</nav>

